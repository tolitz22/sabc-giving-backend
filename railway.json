{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "preDeployCommand": "php artisan migrate --force && php artisan db:seed --class=AdminUserSeeder --force && php artisan db:seed --class=BankAccountSeeder --force",
    "startCommand": "php artisan serve --host=0.0.0.0 --port=8080"
  }
}
